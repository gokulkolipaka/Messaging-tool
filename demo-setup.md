## Quick Local Demo

1. Clone repo
2. `cd server && npm install`
3. `cp .env.example .env`
4. `node server.js`
5. Open `http://localhost:3000`
6. Login with:
   - Phone: `admin`
   - Password: `admin123`
